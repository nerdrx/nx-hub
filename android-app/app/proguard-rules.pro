# Minification is off for release builds (the app is tiny and reflection-free),
# so this file is intentionally almost empty. Kept so the buildType config is
# valid and so enabling minifyEnabled later has a home for rules.
-dontwarn org.json.**
